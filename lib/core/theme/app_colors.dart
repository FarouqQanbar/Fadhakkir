import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const burgundy = Color(0xFF3E0C08);
  static const beige = Color(0xFFCCAB8E);
  static const mutedGreen = Color(0xFF2E3F37);
  static const darkTeal = Color(0xFF042932);
  static const deepNavy = Color(0xFF05192F);

  // Semantic light
  static const lightPrimary = burgundy;
  static const lightOnPrimary = Colors.white;
  static const lightBackground = Color(0xFFF7F3EE); // soft beige
  static const lightSurface = Colors.white;
  static const lightOnSurface = Color(0xFF05192F);

  // Semantic dark
  static const darkPrimary = beige;
  static const darkOnPrimary = deepNavy;
  static const darkBackground = deepNavy;
  static const darkSurface = Color(0xFF072027);
  static const darkOnSurface = beige;
}
