import 'package:flutter/widgets.dart';

class AppRadius {
  static const BorderRadiusGeometry small = BorderRadiusDirectional.all(
    Radius.circular(8),
  );
  static const BorderRadiusGeometry medium = BorderRadiusDirectional.all(
    Radius.circular(12),
  );
  static const BorderRadiusGeometry large = BorderRadiusDirectional.all(
    Radius.circular(20),
  );
}
